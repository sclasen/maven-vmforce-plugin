<project>
    <build>
        <plugins>
            <plugin>
                <groupId>com.force.maven.plugin</groupId>
                <artifactId>maven-vmforce-plugin</artifactId>
                <configuration>
                    <forceScript>${basedir}/target/test-classes/testForceScript.fs</forceScript>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
